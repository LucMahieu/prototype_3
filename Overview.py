import streamlit as st

st.set_page_config(
    page_title="Learnloop",
    page_icon="♾️"
)

st.title("LearnLoop")
st.write(""" 
Using Learnloop, you can grasp topics from your study material more quickly and effectively. 
Learnloop automatically extracts the most important topics and information from your study
material and then creates quizzes to help you internalize the topics more easily.
"""
         )

# st.subheader("Log in")
# user_password = st.text_input("Use your unique code to log in and keep track of your learning progress.", type='password')
# st.button("Enter")

# st.write(user_password)
# Blue circle to indicate that the user logged in: ":large_blue_circle:"
